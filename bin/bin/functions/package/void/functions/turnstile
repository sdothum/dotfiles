#!/usr/bin/dash
# sdothum - 2016 (c) wtfpl
# SEE: xb, .xb_actions

# restore dinit.d
[ "$(ls -1 /etc/dinit.d)" = 'turnstiled' ] ;then
	sudo rm -r /etc/dinit.d
	sudo ln -sv /etc/dinit.d.@chimera /etc/dinit.d
fi

ditto INFO turnstile "check /etc/dinit.d"

# kak: filetype=sh
