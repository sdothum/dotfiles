#!/usr/bin/dash
# sdothum - 2016 (c) wtfpl

# File
# ══════════════════════════════════════════════════════════════════════════════

# ......................................................................... View

DIR=$(dirname $@)
# yazi, nnn or pcmanfm argument passed?
if [ "$DIR" ] ;then
	/usr/bin/feh -B black -d --scale-down --start-at $@ $DIR
else
	/usr/bin/feh -B black -d --scale-down $@
fi

# kak: filetype=sh
