#!/usr/bin/dash
# sdothum - 2016 (c) wtfpl

# File
# ══════════════════════════════════════════════════════════════════════════════

# ......................................................................... View

DIR=$(dirname $@)
# nnn or pcmanfm argument passed?
[ "$DIR" = '' ] || /usr/bin/feh -B black -d --scale-down --start-at $@ $DIR && /usr/bin/feh -B black -d --scale-down $@

# kak: filetype=sh
