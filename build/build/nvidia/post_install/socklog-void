# sdothum - 2016 (c) wtfpl

# Post_install
# ══════════════════════════════════════════════════════════════════════════════

# ....................................................................... syslog

# sudo ln -s /etc/sv/socklog-unix /var/service/
# sudo ln -s /etc/sv/nanoklogd /var/service/
sudo usermod -aG socklog $USER

sv enable socklog-unix
sv enable nanoklogd

# vim: set ft=sh: #
