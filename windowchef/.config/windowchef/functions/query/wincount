# sdothum - 2016 (c) wtfpl
[ $TRACE ] && CHILD="query/wincount $@" . $PARENT

# Windowchef
# ══════════════════════════════════════════════════════════════════════════════

# ................................................................. window count

# Usage: query wincount [name]

wmctrl -l | grep "${1:-.*}" | wc -l

# kak: filetype=sh
