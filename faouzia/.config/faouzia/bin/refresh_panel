#!/usr/bin/dash
# sdothum - 2016 (c) wtfpl
[ $TRACE ] && CHILD="refresh_panel $@" . $PARENT

# Faouzia
# ══════════════════════════════════════════════════════════════════════════════

# ................................................................ Refresh panel

# xtitle -f "X%s" >$(cat $PANEL_FIFO)
# wmctrl -l | grep -q $(pfw) || sirocco window_cycle  # NOTE: clears closed window id on empty desktop
echo "X" >$FIFO/desktop

# kak: filetype=sh
