# sdothum - 2016 (c) wtfpl
# [ $TRACE ] && CHILD="group/add $@" . $PARENT

# Faouzia
# ══════════════════════════════════════════════════════════════════════════════

# ............................................................. Group add window

# Usage: group add <group> [<winid>]

W=${2:-$(pfw 2>/dev/null)} || return  # trap empty group at startup

mkdir -p $GROUP/$1/$W
rm -rf $GROUP:focus/$1/*
mkdir -p $GROUP:focus/$1/$W

# kak: filetype=sh
