# sdothum - 2016 (c) wtfpl
[ $TRACE ] && CHILD="window/rotate $@" . $PARENT

# Faouzia
# ══════════════════════════════════════════════════════════════════════════════

# ................................................................ Rotate window

# Usage: window rotate SEE: window size and window stretch

eval $(xdotool getwindowgeometry -shell $winid)
$NIMCTLR window -- size ${HEIGHT}x${WIDTH}

# kak: filetype=sh
