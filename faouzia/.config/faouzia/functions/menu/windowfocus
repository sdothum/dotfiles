# sdothum - 2016 (c) wtfpl
[ $TRACE ] && CHILD="menu/windowfocus $@" . $PARENT

# Faouzia
# ══════════════════════════════════════════════════════════════════════════════

# ................................................................. Window focus

winids() { wmctrl -l 2>/dev/null | sed 's/ [ -][0-9]/&   ^/' | picklist; }

winid=$(winids | column -s^ -t | rmenu 'Focus window' $cmd -no-custom) || exit

sirocco window focus ${winid%% *}

# kak: filetype=sh
