# sdothum - 2016 (c) wtfpl
[ $TRACE ] && CHILD="layout/revert $@" . $PARENT

# Faouzia
# ══════════════════════════════════════════════════════════════════════════════

# ................................................................ Revert layout

for i in $(sirocco window ids 2>>$WMLOG) ;do
	$NIMCTLR window revert $i
done

sirocco window focus $winid
notify $WM "layout revert <b>ALL</b>"

# kak: filetype=sh
