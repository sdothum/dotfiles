# sdothum - 2016 (c) wtfpl
[ $TRACE ] && CHILD="menu/layout $@" . $PARENT

# Cirrus
# ══════════════════════════════════════════════════════════════════════════════

# ............................................................... Desktop layout

actions() {
	echo "
q ^Quarters
d ^Quarters edit
b ^Quarters browser
h ^Thirds
k ^Thirds edit
j ^Thirds browser
" | picklist
}

cmd=$(actions | rmenu 'Layout' $cmd -no-custom) || exit

cmd=$(echo "$cmd" | sed "s/.*$SEP //")
case $cmd in
	T*browser ) $ZEPHYR layout fold 3 $BROWSER ;;
	T*edit    ) $ZEPHYR layout fold 3 kak      ;;
	Thirds    ) $ZEPHYR layout fold 3          ;;
	Q*browser ) $ZEPHYR layout fold 4 $BROWSER ;;
	Q*edit    ) $ZEPHYR layout fold 4 kak      ;;
	Quarters  ) $ZEPHYR layout fold 4          ;;
esac

# kak: filetype=sh
