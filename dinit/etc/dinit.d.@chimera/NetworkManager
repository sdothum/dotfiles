type            = process
command         = /usr/bin/NetworkManager -d
smooth-recovery = true
logfile         = /var/log/dinit.d/NetworkManager.log
depends-on      = dbus
depends-on      = pre-network.target
before          = network.target
