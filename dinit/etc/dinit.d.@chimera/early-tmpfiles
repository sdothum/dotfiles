# Create volatile files and directories

type       = scripted
command    = /etc/dinit.d/early/scripts/tmpfiles.sh --create --remove --boot --exclude-prefix=/dev
depends-on = early-fs-local.target
depends-on = pre-local.target
