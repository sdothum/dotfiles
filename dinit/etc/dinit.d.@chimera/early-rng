# seed the rng

type         = scripted
command      = /etc/dinit.d/early/scripts/rng.sh start
stop-command = /etc/dinit.d/early/scripts/rng.sh stop
depends-on   = early-devices.target
waits-for    = early-modules.target
waits-for    = early-fs-local.target
