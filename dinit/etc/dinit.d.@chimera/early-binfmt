# Set up configured binary format handlers

type         = scripted
command      = /etc/dinit.d/early/scripts/binfmt.sh start
stop-command = /etc/dinit.d/early/scripts/binfmt.sh stop
depends-on   = early-fs-local.target
