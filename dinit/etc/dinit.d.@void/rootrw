# Re-mount the root filesystem read/write.

type       = scripted
command    = /bin/mount -o remount,rw /
# command  = /etc/dinit.d/scripts/rootrw
restart    = false
options    = starts-rwfs
depends-on = filesystems
waits-for  = console-setup
waits-for  = rootfscheck
